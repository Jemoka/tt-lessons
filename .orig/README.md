# Original lesson snapshots

Verbatim copies of each lesson as first written, captured before the editorial
cleanup pass (remove test-harness coupling, add standalone JAX reproducers,
tighten to concrete bugs, copy-edit prose). The live lessons are the top-level
`YYYY-MM-DD-*` directories; these are kept only for provenance.
